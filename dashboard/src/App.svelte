<script>
  // This shell establishes the component boundary. It is exported by the
  // build but not mounted until the first migrated dashboard section is ready.
  export let title = "LeanRelay";
</script>

<section aria-label={`${title} dashboard migration shell`} hidden></section>
